import Layout from '@/components/Layout';

const Profile = () => (
  <Layout>
    <section className='x-container'>
      <h1>profileeee page</h1>
    </section>
  </Layout>
);

export default Profile;
